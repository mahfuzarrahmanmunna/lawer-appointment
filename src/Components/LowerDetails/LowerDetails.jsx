import React from 'react';
import usePageTitle from '../../Hooks/usePageTitle';

const LowerDetails = () => {
    usePageTitle()
    return (
        <div>

        </div>
    );
};

export default LowerDetails;