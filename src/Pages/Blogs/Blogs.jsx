import React from 'react';
import usePageTitle from '../../Hooks/usePageTitle';

const Blogs = () => {
    usePageTitle()
    return (
        <div>
            Blogs
        </div>
    );
};

export default Blogs;