import React from 'react';
import usePageTitle from '../../Hooks/usePageTitle';

const MyBookings = () => {
    usePageTitle()
    return (
        <div>
            My Bookings
        </div>
    );
};

export default MyBookings;