import React from 'react';
import usePageTitle from '../../Hooks/usePageTitle';

const Home = () => {
    usePageTitle()
    return (
        <div>
            home
        </div>
    );
};

export default Home;